---
description: Auto-push code to GitHub after changes
---

1. After any file modification or task completion:
// turbo
2. Stage all changed files
   `git add .`
// turbo
3. Commit the changes with a concise, descriptive message
   `git commit -m "Auto-update: [Insert brief summary of changes]"`
// turbo
4. Push to the remote branch
   `git push origin main`
